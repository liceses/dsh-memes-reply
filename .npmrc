# 为什么需要这一行（别删，它决定 GitHub 安装能不能成功）
#
# 本包的 lib/ 被 .gitignore 排除，所以 git 依赖必须靠 package.json 的 prepare 脚本现场构建。
# pnpm 在 prepare 时会执行：
#     const pm2 = (await preferredPM(gitRootDir))?.name ?? "npm"
#     manifest.scripts[`${pm2}-install`] = `${pm2} install`
# 仓库里存在 package-lock.json，于是 preferredPM 判定为 npm —— 构建阶段用的是 **npm install**。
#
# 而 devDependencies 里横跨了两个 harness 世代：
#     客户端半边  @deepseek-ai/dsh-client-runtime@0.1.0-rc.8
#                 └─ peer dsh-agent@^0.1.0-rc.8 └─ peer dsh-llm@^0.1.0-rc.8
#     host 半边   @deepseek-ai/dsh-llm@0.1.5-rc.1
# 两者对不上，npm 的严格 peer 解析直接 ERESOLVE 退出，prepare 失败：
#     [ERR_PNPM_PREPARE_PACKAGE] ... npm-install: `npm install` Exit status 1
#
# 这些包在构建期只用来取类型（运行时由宿主 profile 提供），所以放宽 peer 校验是安全的。
# 实测：加上这一行后 ERESOLVE 消失，npm install 装完 92 个包，npm run build 正常产出 lib/。
legacy-peer-deps=true
